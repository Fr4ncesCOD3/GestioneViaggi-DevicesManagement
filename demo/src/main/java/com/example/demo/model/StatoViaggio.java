// Questo è il package che contiene la classe
package com.example.demo.model;

// Questa è una enumerazione (enum) che definisce i possibili stati di un viaggio
// Un enum è un tipo speciale che può avere solo un insieme fisso di valori
public enum StatoViaggio {
    // Questi sono i valori possibili per lo stato di un viaggio:
    
    // IN_PROGRAMMA: indica che il viaggio è pianificato ma non è ancora avvenuto
    IN_PROGRAMMA,
    // COMPLETATO: indica che il viaggio è stato effettuato e concluso
    COMPLETATO
} 